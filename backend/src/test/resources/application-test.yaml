spring:
  flyway:
    enabled: true
  jpa:
    hibernate:
      ddl-auto: validate

app:
  notifications:
    rabbit:
      enabled: false
    emailjs:
      enabled: false
    zapsender:
      enabled: false
